@model List<User>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (User user in @Model)
        {
            <tr>
                <td>@user.FirstName</td>
                <td>@user.LastName</td>
                <td>@user.Email</td>
                <td>
                    <a asp-controller="Admin" asp-action="UserDetail" asp-route-id="@user.Id">Edit/View</a>
                </td>
            </tr>
        }
    </tbody>
</table>